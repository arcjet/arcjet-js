## API Report File for "@arcjet/fastify"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ArcjetDecision } from 'arcjet';
import { ArcjetMetadata } from 'arcjet';
import { ArcjetOptions as ArcjetOptions_2 } from 'arcjet';
import { ArcjetRule } from 'arcjet';
import { CharacteristicProps } from 'arcjet';
import { cloudflare } from '@arcjet/ip';
import { createClient } from '@arcjet/protocol/client.js';
import { ExtraProps } from 'arcjet';
import { Primitive } from 'arcjet';
import { Product } from 'arcjet';
import { ProxyService } from '@arcjet/ip';

// @public
function arcjet<const Rules extends (Primitive | Product)[], const Characteristics extends readonly string[]>(options: ArcjetOptions<Rules, Characteristics>): ArcjetFastify<ExtraProps<Rules> & CharacteristicProps<Characteristics>>;
export default arcjet;

// @public
export interface ArcjetFastify<Props> {
    // Warning: (ae-forgotten-export) The symbol "MaybeProperties" needs to be exported by the entry point index.d.ts
    protect(request: ArcjetFastifyRequest, ...properties: MaybeProperties<Props & {
        correlationId?: string;
        metadata?: ArcjetMetadata;
    }>): Promise<ArcjetDecision>;
    withRule<Rule extends ArcjetRule>(rule: Array<Rule>): ArcjetFastify<Props & (Rule extends ArcjetRule<infer P> ? P : {})>;
}

// @public
export interface ArcjetFastifyRequest {
    body: unknown;
    headers: Record<string, Array<string> | string | undefined>;
    method: string;
    protocol: "https" | "http";
    server: {
        initialConfig?: {
            https?: unknown;
        } | undefined;
    };
    socket: {
        encrypted?: boolean | undefined;
        remoteAddress?: string | undefined;
    };
    url: string;
}

// @public
export type ArcjetOptions<Rules extends [...Array<Primitive | Product>], Characteristics extends ReadonlyArray<string>> = ArcjetOptions_2<Rules, Characteristics> & {
    proxies?: ReadonlyArray<string | ProxyService> | null | undefined;
};

export { cloudflare }

// @public
export function createRemoteClient(options?: RemoteClientOptions | null | undefined): ReturnType<typeof createClient>;

export { ProxyService }

// @public
export type RemoteClientOptions = {
    baseUrl?: string;
    timeout?: number;
};


export * from "arcjet";

// (No @packageDocumentation comment for this package)

```
