## API Report File for "@arcjet/astro"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { AstroIntegration } from 'astro';
import { BotOptions } from 'arcjet';
import { cloudflare } from '@arcjet/ip';
import { DetectPromptInjectionOptions } from 'arcjet';
import { EmailOptions } from 'arcjet';
import { FilterOptions } from 'arcjet';
import { FixedWindowRateLimitOptions } from 'arcjet';
import { ProtectSignupOptions } from 'arcjet';
import { ProxyService } from '@arcjet/ip';
import { SensitiveInfoOptions } from 'arcjet';
import { ShieldOptions } from 'arcjet';
import { SlidingWindowRateLimitOptions } from 'arcjet';
import { TokenBucketRateLimitOptions } from 'arcjet';

// @public
function arcjet<Characteristics extends readonly string[]>(options?: ArcjetOptions<Characteristics>): AstroIntegration;
export default arcjet;

// @public
export type ArcjetOptions<Characteristics extends readonly string[]> = {
    rules: IntegrationRule<Characteristics>[];
    characteristics?: Characteristics;
    client?: RemoteClientOptions;
    proxies?: Array<string | ProxyService>;
};

export { cloudflare }

// @public
export function createRemoteClient(options?: RemoteClientOptions | undefined): {
    readonly baseUrl: string | undefined;
    readonly timeout: number | undefined;
};

// @public
export function detectBot(options: BotOptions): {
    readonly type: "bot";
    readonly options: BotOptions;
};

// @public
export function detectPromptInjection(options?: DetectPromptInjectionOptions): {
    readonly type: "detectPromptInjection";
    readonly options: DetectPromptInjectionOptions;
};

// @public @deprecated
export const experimental_detectPromptInjection: typeof detectPromptInjection;

// @public
export function filter(options: FilterOptions): {
    readonly type: "filter";
    readonly options: FilterOptions;
};

// @public
export function fixedWindow<Characteristics extends readonly string[]>(options: FixedWindowRateLimitOptions<Characteristics>): {
    readonly type: "fixedWindow";
    readonly options: FixedWindowRateLimitOptions<Characteristics>;
};

// @public
export function protectSignup<Characteristics extends readonly string[]>(options: ProtectSignupOptions<Characteristics>): {
    readonly type: "protectSignup";
    readonly options: ProtectSignupOptions<Characteristics>;
};

export { ProxyService }

// @public
export type RemoteClientOptions = {
    baseUrl?: string | undefined;
    timeout?: number | undefined;
};

// @public
export function sensitiveInfo(options: SensitiveInfoOptions<never>): {
    readonly type: "sensitiveInfo";
    readonly options: SensitiveInfoOptions<never>;
};

// @public
export function shield(options: ShieldOptions): {
    readonly type: "shield";
    readonly options: ShieldOptions;
};

// @public
export function slidingWindow<Characteristics extends readonly string[]>(options: SlidingWindowRateLimitOptions<Characteristics>): {
    readonly type: "slidingWindow";
    readonly options: SlidingWindowRateLimitOptions<Characteristics>;
};

// @public
export function tokenBucket<Characteristics extends readonly string[]>(options: TokenBucketRateLimitOptions<Characteristics>): {
    readonly type: "tokenBucket";
    readonly options: TokenBucketRateLimitOptions<Characteristics>;
};

// @public
export function validateEmail(options: EmailOptions): {
    readonly type: "email";
    readonly options: EmailOptions;
};

// Warnings were encountered during analysis:
//
// dist/index.d.ts:49:3 - (ae-forgotten-export) The symbol "IntegrationRule" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
