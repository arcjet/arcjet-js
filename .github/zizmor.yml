# zizmor static-analysis config. See https://docs.zizmor.sh/configuration/

rules:
  cache-poisoning:
    # These workflows trigger on push-to-main, which matches zizmor's
    # "publishing" heuristic, but they only run tests — no artifacts are
    # published.
    ignore:
      - check-skills.yml
      - guard.yml
