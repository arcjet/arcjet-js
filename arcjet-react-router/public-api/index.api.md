## API Report File for "@arcjet/react-router"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ArcjetDecision } from 'arcjet';
import { ArcjetMetadata } from 'arcjet';
import { ArcjetOptions as ArcjetOptions_2 } from 'arcjet';
import { ArcjetRule } from 'arcjet';
import { CharacteristicProps } from 'arcjet';
import { Client as Client_2 } from '@arcjet/protocol/client.js';
import { cloudflare } from '@arcjet/ip';
import { ExtraProps } from 'arcjet';
import { Primitive } from 'arcjet';
import { Product } from 'arcjet';
import { ProxyService } from '@arcjet/ip';

// @public
function arcjet<Rules extends Array<Primitive | Product>, Characteristics extends ReadonlyArray<string>>(options: ArcjetOptions<Rules, Characteristics>): ArcjetReactRouter<ExtraProps<Rules> & CharacteristicProps<Characteristics>>;
export default arcjet;

// @public
export interface ArcjetOptions<Rules extends Array<Primitive | Product>, Characteristics extends ReadonlyArray<string>> extends ArcjetOptions_2<Rules, Characteristics> {
    proxies?: ReadonlyArray<string | ProxyService> | null | undefined;
}

// @public
export interface ArcjetReactRouter<Properties extends Record<PropertyKey, unknown>> {
    // Warning: (ae-forgotten-export) The symbol "MaybeProperties" needs to be exported by the entry point index.d.ts
    protect(details: ArcjetReactRouterRequest, ...rest: MaybeProperties<Properties & {
        correlationId?: string;
        metadata?: ArcjetMetadata;
    }>): Promise<ArcjetDecision>;
    withRule<Rule extends ArcjetRule>(rule: Array<Rule>): ArcjetReactRouter<Properties & (Rule extends ArcjetRule<infer P> ? P : {})>;
}

// @public
export interface ArcjetReactRouterRequest {
    context?: unknown;
    request: Request;
}

export { cloudflare }

// @public
export function createRemoteClient(options?: RemoteClientOptions | null | undefined): Client_2;

export { ProxyService }

// @public
export interface RemoteClientOptions {
    baseUrl?: string | null | undefined;
    timeout?: number | null | undefined;
}


export * from "arcjet";

// (No @packageDocumentation comment for this package)

```
