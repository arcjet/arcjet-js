<h1>This page is rate limited</h1>
<p>
	The rate limiting is configured to allow no more than 5 requests in the last 10 seconds. That's 5
	requests from this page or the <a href="/api/rate-limited">the rate-limited API endpoint</a> combined.
</p>
<p>
	Go ahead and hit refresh a few times to see it in action. Then look at
	<code>/src/routes/rate-limited/+page.server.ts</code> to see how it's done.
</p>
